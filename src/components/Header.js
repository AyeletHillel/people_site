import { Link } from "react-router-dom";

function Component(promp) {
    return <nav className="nav">
        <Link to="/">
            <div>People App</div>
        </Link>
        </nav>
};

export default Component; 